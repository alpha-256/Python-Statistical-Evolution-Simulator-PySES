from statsgen import statsControl
from offspringsys import offspringSys
from generation1 import firstGen
rr_sum = 0
generation = 0
pop = int(input("Population? "))
firstGen.pt1(f, rr, mr, pop)
print(generation)
